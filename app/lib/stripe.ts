import Stripe from 'stripe'; export function getStripe(){ const key=process.env.STRIPE_SECRET_KEY||''; if(!key) throw new Error('STRIPE_SECRET_KEY not set'); return new Stripe(key,{apiVersion:'2024-06-20'} as any);} export function squarePriceCents(){ return parseInt(process.env.SQUARE_PRICE_CENTS||'1000',10);}
